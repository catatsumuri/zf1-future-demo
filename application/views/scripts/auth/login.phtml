<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h1 class="panel-title">Log in</h1>
            </div>
            <div class="panel-body">
                <p class="text-muted">デモ用アカウントでログインすると、ナビゲーションにユーザー名が表示されます。</p>
                <?php if (!empty($this->error)) : ?>
                    <div class="alert alert-danger" role="alert">
                        <?php echo $this->escape($this->error); ?>
                    </div>
                <?php endif; ?>
                <form method="post" action="<?php echo $this->url(['controller' => 'auth', 'action' => 'login'], null, true); ?>">
                    <div class="form-group">
                        <label for="login-email">Email address</label>
                        <input type="email" id="login-email" name="email" class="form-control" placeholder="you@example.com" value="<?php echo $this->escape((string) $this->email); ?>" required autofocus>
                    </div>
                    <div class="form-group">
                        <label for="login-password">Password</label>
                        <input type="password" id="login-password" name="password" class="form-control" placeholder="********" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Sign in</button>
                </form>
            </div>
        </div>
    </div>
</div>
