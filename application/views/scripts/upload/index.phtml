<div class="row">
    <div class="col-md-6">
        <div class="panel panel-default">
            <div class="panel-heading">ファイルをアップロード</div>
            <div class="panel-body">
                <form method="post" enctype="multipart/form-data">
                    <div class="form-group">
                        <label class="control-label" for="upload">ファイル</label>
                        <input type="file" name="upload" id="upload" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-primary">アップロード</button>
                </form>
                <p class="help-block">保存先: <?php echo $this->escape($this->storageDescription); ?></p>
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">アップロード済みファイル</div>
            <div class="panel-body">
                <?php if (empty($this->files)) : ?>
                    <p class="text-muted">まだファイルはありません。</p>
                <?php else : ?>
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>ファイル名</th>
                                    <th class="text-right">サイズ (bytes)</th>
                                    <th>更新日時</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($this->files as $file) : ?>
                                    <tr>
                                        <td><?php echo $this->escape($file['name']); ?></td>
                                        <td class="text-right"><?php echo number_format((int) $file['size']); ?></td>
                                        <td><?php echo date('Y-m-d H:i:s', (int) $file['mtime']); ?></td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
